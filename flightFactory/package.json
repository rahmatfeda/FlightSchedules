{
    "name": "flight",
    "version": "0.0.0",
    "description": "A module for keeping track of a flight",
    "main": "flightFactory.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    }
  }